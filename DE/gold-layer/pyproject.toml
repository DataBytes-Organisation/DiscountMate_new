[project]
name = "discount-mate-gold-layer"
version = "0.1.0"
description = "dbt models for the Discount Mate PostgreSQL gold layer."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dbt-postgres>=1.10,<2",
]

[tool.uv]
package = false

